# Copy to ../secrets.yaml (repo root) before running mqttpi.
#   cp secrets.example.yaml ../secrets.yaml
#
# secrets.yaml is gitignored. Merged onto config.yaml at runtime.

mqtt:
  username: mqtt_user
  password: "change-me"